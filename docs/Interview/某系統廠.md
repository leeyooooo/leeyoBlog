# 系統廠

先自我介紹後問經驗內容

開始四題考題

結束後回答觀念問題

## git

- 可以從自己要開發一個feature的到上prod的流程說出來嗎?
- 如果多人同時平行開發怎麼管理?
- 開PR有被reject過嗎 什麼狀況下會被reject?
- 如果今天你的code跟人家有conflict如何解決

## UI test

- 用什麼套件來測試UI?
A:storybook
- 有聽過selenium嗎?

## es-lint

- 平常習慣 airbnb還是react模式?
- 有沒有覺得airbnb好或不好的地方?

## Redux

你們平常是如何做state的保存跟管理的?

## Live Coding

- 1.把class component 換成 function component 並且搭配react hook

    ```jsx
    import React, { useState, useEffect } from "react";
    import { Button } from "antd";
    import "./test.scss";

    /**
     * 請將下列程式碼轉換為 Functional component 搭配 Hooks
     */
    class Test extends Component {
      constructor() {
        super();
        this.state = {
          data: []
        };
        this.handleClick = this.handleClick.bind(this);
      }

      componentDidMount() {
        this.setState({
          data: [
            { index: 1, label: "AAAA" },
            { index: 2, label: "BBBB" },
            { index: 3, label: "CCCC" }
          ]
        });
      }

      handleClick() {
        this.setState({
          data: [
            { index: 4, label: "DDDD" },
            { index: 5, label: "EEEE" },
            { index: 6, label: "FFFF" }
          ]
        });
      }

      render() {
        const { data } = this.state;
        return (
          <div className="test">
            {data.map((item) => {
              const { index, label } = item;
              return (
                <div className="test__item" key={index}>{`${index}、${label}`}</div>
              );
            })}
            <Button type="primary" onClick={this.handleClick}>
              Change List
            </Button>
          </div>
        );
      }
    }
    /*
    A:
    const Test = () => {
      const initData = [
        { index: 1, label: "AAAA" },
        { index: 2, label: "BBBB" },
        { index: 3, label: "CCCC" }
      ];
      const [data, setData] = useState([]);

      const handleClick = () => {
        setData([
          { index: 4, label: "DDDD" },
          { index: 5, label: "EEEE" },
          { index: 6, label: "FFFF" }
        ]);
      };

      useEffect(() => {
        setData(initData)
      },[data])

      return (
        <div className="test">
          {data.map((item) => {
            const { index, label } = item;
            return (
              <div className="test__item" key={index}>{`${index}、${label}`}</div>
            );
          })}
          <Button type="primary" onClick={handleClick}>
            Change List
          </Button>
        </div>
      );
    };
    */

    export default Test;
    ```

- 2.給了一段大量的迴圈
說明這段程式碼有什麼問題
並且用useMemo去改善這段程式碼
且說明useMemo可以做什麼

    ```jsx
    import React, { useMemo } from "react";

    import "./test.scss";

    const reuquestData = ["AAAA", "BBBB", "CCCC"];

    /**
     * 1. 請說明下列程式碼哪邊不好
     * 2. 請透過 useMemo 優化程式碼
     * 3. 說明 useMemo 為何可以優化效能?
     */

    const Test = () => {
      const data = [];

       /*
    		A:const renderList = useMemo(()=>{
        for (let i = 0; i < reuquestData.length; i += 1) {
          data.push({ index: i, label: reuquestData[i] });
        }
        return data
       },[reuquestData])
    		*/

    	for (let i = 0; i < reuquestData.length; i += 1) {
          data.push({ index: i, label: reuquestData[i] });
        }


      return (
        <div className="test">
          {renderList.map((item) => {
            const { index, label } = item;
            return (
              <div className="test__item" key={index}>{`${index}、${label}`}</div>
            );
          })}
        </div>
      );
    };

    export default Test;
    ```

- 3.把資料按照所要要求更改

    ```jsx
    import React, { useMemo } from "react";
    import { Table } from "antd";

    import "./test.scss";

    const persons = [
      { gender: "male", name: "James", age: 16 },
      { gender: "male", name: "John", age: 22 },
      { gender: "female", name: "Linda", age: 85 },
      { gender: "female", name: "Jessica", age: 10 },
      { gender: "male", name: "Michael", age: 15 },
      { gender: "male", name: "Robert", age: 44 },
      { gender: "female", name: "Mary", age: 21 },
      { gender: "male", name: "Jason", age: 33 },
      { gender: "female", name: "Patricia", age: 74 }
    ];

    const Test = () => {
      const dataSource = useMemo(() => {
        /**
         * 1. 篩選出"男性"
         * 2. 依照年紀大小，由大到小排列出來
         */
       /*A: const maleList = persons.filter((person)=>{
          return person.gender === "male"
        });
        const result = maleList.sort((a,b)=>{
          return b.age - a.age
        })*/
        // .... 請於此處撰寫程式碼

        return result;
      }, [persons]);

      return (
        <div className="test">
          <Table
            bordered
            dataSource={dataSource}
            columns={Object.keys(dataSource?.[0]).map((key) => ({
              title: key,
              dataIndex: key,
              key
            }))}
            rowKey="name"
            pagination={false}
          />
        </div>
      );
    };

    export default Test;
    ```

- 4.從父層拿取子層的原件

    ```jsx
    import React from "react";
    import "./test.scss";

    /**
     * 請在 Parent component 裡面顯示出 Child component content
     */

    function Child() {
      return <h2>This is children content</h2>;
    }
    ;
    function Parent() {//A:從props裡面的children拿
      **return (
        <div>
          <h1>Parent Component</h1>
          {children}
        </div>
      );
    }

    const Test = () => {
      return (
        <Parent>
          <Child />
        </Parent>
      );
    };

    export default Test;
    ```


# 面試心得:

這次的題目都還蠻實際的也都還蠻基本的都有答出來，不過一些細節像是dependencies的部分忘記修改有被提示。
然後就是第四題竟然突然腦袋空白不知道是不是寫style component太久突然忘記了😟
被提示兩次才突然想到答案...…整體來說是很開心的面試有學到東西面試官也很熱情地給反饋